Installation
----------------------

### Install with `pip`
This is the recommended way to install Limeutils.

For `pip` users
```
pip install limeutils
```
For `pipenv` users
```
pipenv install limeutils
```

### Install with `git`

### Install a fork
